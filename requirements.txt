# Runtime dependencies for SysForge (mirrors [project] dependencies in pyproject.toml).
# Editable install with dev tools: python -m pip install -e ".[dev]"

typer>=0.12
markdown>=3.6
pygments>=2.18
psutil>=5.9
rich>=13.7
